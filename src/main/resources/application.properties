spring.jpa.database=postgresql
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect = org.hibernate.spatial.dialect.postgis.PostgisDialect

spring.datasource.username = postgres
spring.datasource.password = qwe123qwe123
# I use a Docker instance for containing the database,
# The docker pull is mdillon/postgis:LATEST
# See the dockerSetupRunPostgisDatabaseScript for commands
spring.datasource.url = jdbc:postgresql://localhost:5444/postgres
spring.datasource.driver-class-name=org.postgresql.Driver